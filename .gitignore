# Claude Code local settings
.claude/

# OS
.DS_Store
Thumbs.db

# VPS config local (contiene IP, usuario y credenciales — no subir al repo)
vps/vps.config.bat
vps/backend.env.production

# Firebase service account keys — NUNCA subir al repo
*firebase-adminsdk*.json
firebase-key.json

# Docker compose de producción — tiene credenciales, se maneja en VPS
docker-compose.yml
